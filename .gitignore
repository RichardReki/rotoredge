# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Secrets (CMC API key is for the LIVE skill path only; never commit it)
.env
*.key

# Raw download scratch (the FROZEN snapshot under data/snapshot/ IS committed for keyless repro)
data/raw/
*.zip
.tmp/

# OS
.DS_Store
Thumbs.db
